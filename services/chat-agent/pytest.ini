[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    integration: hits one real external dependency (Qdrant, Ollama, or mcp-server); skipped when dep is down
    e2e: reserved — frontend e2e tests live in dashboard/e2e/ and run via Playwright
